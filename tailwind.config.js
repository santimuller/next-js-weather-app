const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  purge: [],
  theme: {
    colors: {
      black: colors.black,
      white: colors.white,
      gray: colors.gray,
      indigo: colors.indigo,
      purple: colors.purple,
      red: colors.red,
      green: colors.green,
    },
  },
  variants: {},
  plugins: [],
}
